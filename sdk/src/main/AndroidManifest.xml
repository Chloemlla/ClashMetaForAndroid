<?xml version="1.0" encoding="utf-8"?>
<!--
  Runtime SDK is a pure library facade. Service / provider components are merged from
  the :service AAR. RECEIVE_SELF_BROADCASTS is declared in the :common module's manifest
  (common/src/main/AndroidManifest.xml) and is merged into the host automatically through
  the dependency on :sdk — hosts do not need to redeclare it. VPN / FOREGROUND_SERVICE
  permissions are required by the platform as applicable.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />