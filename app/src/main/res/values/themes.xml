<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="BootstrapTheme" parent="AppThemeLight" />

    <!-- Floating host for the pairing prompt: the activity window stays invisible so the
         MaterialAlertDialog is what the user sees, centered over whatever is behind it. -->
    <style name="PartnerPairingTheme" parent="AppThemeLight">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
