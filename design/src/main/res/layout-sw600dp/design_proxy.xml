<?xml version="1.0" encoding="utf-8"?>
<!--
    Large-screen (sw600dp+) Proxy dual-pane host (M4 R1).

    The detail pane below is a byte-for-byte copy of the default `design_proxy.xml` content
    (same ids, same view types) so ProxyDesign/ProxyPageAdapter/ProxyAdapter continue to own
    all business logic (search / sort / progressive delay / selection) unchanged. The only
    additive piece is the group-list rail on the start side, which is a pure navigation
    shortcut over the existing ViewPager2 pages.
-->
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="self"
            type="com.github.kr328.clash.design.ProxyDesign" />
    </data>

    <androidx.slidingpanelayout.widget.SlidingPaneLayout
        android:id="@+id/sliding_pane_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- List pane: proxy group names. Fixed width; screen-start inset lives here since
             this pane is now the one flush against the physical screen edge. -->
        <LinearLayout
            android:layout_width="@dimen/proxy_group_list_pane_width"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:paddingStart="@{self.surface.insets.start}"
            android:background="?android:windowBackground">

            <TextView
                android:id="@+id/group_list_title_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="@dimen/toolbar_height"
                android:gravity="center_vertical"
                android:paddingStart="@dimen/item_header_margin"
                android:paddingEnd="@dimen/item_header_margin"
                android:paddingTop="@{self.surface.insets.top}"
                android:text="@string/proxy"
                android:textAppearance="@style/TextAppearance.MaterialComponents.Headline6" />

            <View
                android:layout_width="match_parent"
                android:layout_height="@dimen/divider_size"
                android:background="?android:windowBackground"
                android:alpha="0.96" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/group_list_view"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:clipToPadding="false"
                android:paddingBottom="@{self.surface.insets.bottom}" />
        </LinearLayout>

        <!-- Detail pane: unmodified copy of the phone (default) design_proxy.xml content. -->
        <androidx.coordinatorlayout.widget.CoordinatorLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:paddingEnd="@{self.surface.insets.end}">

            <TextView
                android:id="@+id/empty_view"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="@string/proxy_empty_tips"
                android:textAppearance="@style/TextAppearance.MaterialComponents.Headline6"
                android:visibility="gone" />

            <androidx.viewpager2.widget.ViewPager2
                android:id="@+id/pages_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="horizontal" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <com.github.kr328.clash.design.view.ActivityBarLayout
                    android:id="@+id/activity_bar_layout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingTop="@{self.surface.insets.top}">

                    <RelativeLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:paddingEnd="@dimen/item_tailing_margin">

                        <include
                            layout="@layout/common_activity_bar"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_alignParentStart="true"
                            android:layout_centerVertical="true"
                            android:layout_toStartOf="@id/url_test_layout" />

                        <FrameLayout
                            android:id="@+id/url_test_layout"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_centerVertical="true"
                            android:layout_marginStart="@dimen/item_tailing_margin"
                            android:layout_toStartOf="@id/search_view">

                            <ImageView
                                android:id="@+id/url_test_view"
                                android:layout_width="@dimen/toolbar_image_action_size"
                                android:layout_height="@dimen/toolbar_image_action_size"
                                android:layout_gravity="center"
                                android:background="?attr/selectableItemBackgroundBorderless"
                                android:clickable="true"
                                android:contentDescription="@string/delay_test"
                                android:focusable="true"
                                android:onClick="@{() -> self.requestUrlTesting()}"
                                android:padding="@dimen/toolbar_image_action_padding"
                                android:src="@drawable/ic_baseline_flash_on" />

                            <ProgressBar
                                android:id="@+id/url_test_progress_view"
                                android:layout_width="@dimen/item_tailing_component_size"
                                android:layout_height="@dimen/item_tailing_component_size"
                                android:visibility="gone" />
                        </FrameLayout>

                        <ImageView
                            android:id="@+id/search_view"
                            android:layout_width="@dimen/toolbar_image_action_size"
                            android:layout_height="@dimen/toolbar_image_action_size"
                            android:layout_centerVertical="true"
                            android:layout_marginStart="@dimen/item_tailing_margin"
                            android:layout_toStartOf="@id/menu_view"
                            android:background="?attr/selectableItemBackgroundBorderless"
                            android:clickable="true"
                            android:contentDescription="@string/search"
                            android:focusable="true"
                            android:padding="@dimen/toolbar_image_action_padding"
                            android:src="@drawable/ic_baseline_search" />

                        <ImageView
                            android:id="@+id/menu_view"
                            android:layout_width="@dimen/toolbar_image_action_size"
                            android:layout_height="@dimen/toolbar_image_action_size"
                            android:layout_alignParentEnd="true"
                            android:layout_centerVertical="true"
                            android:layout_marginStart="@dimen/item_tailing_margin"
                            android:background="?attr/selectableItemBackgroundBorderless"
                            android:clickable="true"
                            android:contentDescription="@string/more"
                            android:focusable="true"
                            android:padding="@dimen/toolbar_image_action_padding"
                            android:src="@drawable/ic_baseline_more_vert" />

                    </RelativeLayout>
                </com.github.kr328.clash.design.view.ActivityBarLayout>

                <com.google.android.material.tabs.TabLayout
                    android:id="@+id/tab_layout_view"
                    android:layout_width="match_parent"
                    android:layout_height="@dimen/tab_layout_height"
                    android:alpha="0.96"
                    android:background="?android:windowBackground"
                    app:tabBackground="@android:color/transparent"
                    app:tabMode="scrollable" />

                <LinearLayout
                    android:id="@+id/search_bar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:alpha="0.96"
                    android:background="?android:windowBackground"
                    android:gravity="center_vertical"
                    android:orientation="horizontal"
                    android:paddingStart="@dimen/item_header_margin"
                    android:paddingEnd="@dimen/item_tailing_margin"
                    android:paddingBottom="@dimen/item_text_margin"
                    android:visibility="gone">

                    <androidx.appcompat.widget.AppCompatEditText
                        android:id="@+id/keyword_view"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:hint="@string/search_proxy_hint"
                        android:imeOptions="actionSearch"
                        android:inputType="text"
                        android:maxLines="1"
                        android:singleLine="true" />

                    <ImageView
                        android:id="@+id/clear_search_view"
                        android:layout_width="@dimen/toolbar_image_action_size"
                        android:layout_height="@dimen/toolbar_image_action_size"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:clickable="true"
                        android:contentDescription="@string/close"
                        android:focusable="true"
                        android:padding="@dimen/toolbar_image_action_padding"
                        android:src="@drawable/ic_baseline_close" />
                </LinearLayout>

                <View
                    android:id="@+id/elevation_view"
                    android:layout_width="match_parent"
                    android:layout_height="@dimen/divider_size"
                    android:alpha="0.96"
                    android:background="?android:windowBackground"
                    android:elevation="@dimen/tab_layout_elevation" />
            </LinearLayout>

            <FrameLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end|bottom"
                android:paddingBottom="@{self.surface.insets.bottom}">

                <com.google.android.material.floatingactionbutton.FloatingActionButton
                    android:id="@+id/url_test_float_view"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_margin="@dimen/item_tailing_margin"
                    android:contentDescription="@string/delay_test"
                    android:onClick="@{() -> self.requestUrlTesting()}"
                    android:src="@drawable/ic_baseline_flash_on" />
            </FrameLayout>
        </androidx.coordinatorlayout.widget.CoordinatorLayout>
    </androidx.slidingpanelayout.widget.SlidingPaneLayout>
</layout>
